<Window x:Class="MVVM_Community_Toolkit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVM_Community_Toolkit"
        xmlns:vm="clr-namespace:MVVM_Community_Toolkit"
        mc:Ignorable="d"
        Title="CommunityToolkit MVVM" Height="250" Width="350">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Name:" Grid.Row="0"/>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="1"
                 Margin="0,5,0,10"/>

        <TextBlock Text="Surname:" Grid.Row="2"/>
        <TextBox Text="{Binding Surname, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="3"
                 Margin="0,5,0,10"/>

        <Button Content="Submit"
                Command="{Binding SubmitCommand}"
                Grid.Row="4"
                Width="100"
                Height="30"/>

        <Label Content="{Binding Result}"
               Grid.Row="5"
               Margin="0,50,0,0"
               FontWeight="Bold"/>
    </Grid>
</Window>
